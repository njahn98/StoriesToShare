import React from 'react';
import { Button } from 'react-bootstrap';

function CreateButton({ variant }) {
    return (
        <Button variant={variant}>Create</Button>
    );
}
export default CreateButton